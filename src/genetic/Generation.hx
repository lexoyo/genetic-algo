package pathfinder;

class Generation {
  private var creatures : Array<Creature>;
  private function new ( creatures : Array<Creature> ) {
    this.creatures = creatures;
  }
  public static function getRandom( numCreatures : Int ) : Generation {
    var creatures : Array<Creature>() = [ for (i in 0...numCreatures) {
      var genotype : Array<Chromosome> = Chromosome.);
      Creature.
    }];
  }
  public function getNext() : Generation {
  }
  public function toString() : String {
  }
}
